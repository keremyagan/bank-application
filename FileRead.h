#ifndef FileRead_H
#define FileRead_H
#include <iostream>
using namespace std;

class FileRead{
	// Private section
	public:
		string file_name;
		typedef struct Info Struct;
		Struct file_read();
	protected:
		// Protected Declarations
};

#endif
